{
  "name": "nodejs-express-on-aws-ec2",
  "version": "0.0.1",
  "scripts": {
    "start": "node index.js",  // Adjust this if your entry point is different
    "test": "echo \"Error: no test specified\" && exit 1", // Placeholder for test command
    "build": "echo 'Building project...'" // This is a placeholder build script
  },
  "dependencies": {
    "express": "4.17.1"
  }
}
